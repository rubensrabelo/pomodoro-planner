export const API_VERSION = "/api/v1";

export const ROUTES = {
  tags: `${API_VERSION}/tags`,
  tasks: `${API_VERSION}/tasks`,
  pomodoros: `${API_VERSION}/pomodoros`,
  docs: `${API_VERSION}/api-docs`, // Testar se o swagger está sendo gerado
};